<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:james="https://jamesnet.dev/xaml/presentation"
    xmlns:units="clr-namespace:AnatomyAnalyzer.UI.Units">

    <Style TargetType="{x:Type units:ControlTreeView}">
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Background" Value="{DynamicResource ControlTree.Background}"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type units:ControlTreeView}">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="0">
                        <james:JamesGrid Rows="Auto, *" Columns="*">
                            <TextBlock Text="{DynamicResource Controls}"
                                       FontSize="14"
                                       FontFamily="Roboto"
                                       FontWeight="SemiBold"
                                       Visibility="Collapsed"
                                       Padding="10"/>
                            <ItemsPresenter/>
                        </james:JamesGrid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
